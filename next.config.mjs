// next.config.mjs
import withVideos from 'next-videos';

/** @type {import('next').NextConfig} */
const nextConfig = {};

// Export the configuration using withVideos
export default withVideos(nextConfig);